language: node_js
node_js:
  - 10 # use nodejs v10 LTS

branches:
  only:
    - hexo-project

install:
- npm ci
# keep the npm cache around to speed up installs
cache:	
  directories:	
  - "$HOME/.npm"	

script:
  - hexo generate

deploy:
  provider: pages
  skip-cleanup: true